// This file was automatically generated using the 'generateCode' Gradle task. Do not edit manually.
package io.fluidsonic.i18n.`data`

import kotlin.Int
import kotlin.String

internal fun localizedNameForRegion_short_ti(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    139 -> "ሆንግ ኮንግ" // HK
    193 -> "ማካው" // MO
    229 -> "ፍልስጤም" // PS
    297 -> "ሕ.መ." // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_tk(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    139 -> "Gonkong" // HK
    193 -> "Makao" // MO
    229 -> "Palestina" // PS
    297 -> "ABŞ" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_to(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    139 -> "Hongi Kongi" // HK
    193 -> "Makau" // MO
    229 -> "Palesitaine" // PS
    297 -> "ʻAmelika" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_tok(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "ma Juke" // GB
    229 -> "ma Palasin" // PS
    else -> null
  }
}

internal fun localizedNameForRegion_short_tr(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "BK" // GB
    139 -> "Hong Kong" // HK
    193 -> "Makao" // MO
    229 -> "Filistin" // PS
    296 -> "BM" // UN
    297 -> "ABD" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_trw(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "یو کے سلطنت متحدہ" // GB
    139 -> "ہانگ کانگ ہانگ کانگ SAR چین" // HK
    193 -> "مکاؤ مکاؤ SAR چین" // MO
    229 -> "فلسطین فلسطینی خطے" // PS
    297 -> "امریکا" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_tt(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    139 -> "Гонконг" // HK
    193 -> "Макао" // MO
    229 -> "Фәләстин" // PS
    else -> null
  }
}

internal fun localizedNameForRegion_short_tyv(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    297 -> "АКШ" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_ug(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "ئەنگلىيە" // GB
    139 -> "شياڭگاڭ" // HK
    193 -> "ئاۋمېن" // MO
    229 -> "پەلەستىن" // PS
    297 -> "ئا ق ش" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_uk(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    139 -> "Гонконг" // HK
    193 -> "Макао" // MO
    229 -> "Палестина" // PS
    296 -> "ООН" // UN
    297 -> "США" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_ur(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "یو کے" // GB
    139 -> "ہانگ کانگ" // HK
    193 -> "مکاؤ" // MO
    229 -> "فلسطین" // PS
    297 -> "امریکا" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_uz(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? = when (region) {
  // region = <none>
  0 -> when (script) {
    // uz
    0 -> when (variant == 0) {
      true -> when (query) {
        121 -> "Britaniya" // GB
        139 -> "Gonkong" // HK
        193 -> "Makao" // MO
        229 -> "Falastin" // PS
        296 -> "BMT" // UN
        297 -> "AQSH" // US
        else -> null
      }
      false -> null
    }
    // uz-Cyrl
    2 -> when (variant == 0) {
      true -> when (query) {
        139 -> "Гонконг" // HK
        193 -> "Макао" // MO
        229 -> "Фаластин" // PS
        297 -> "АҚШ" // US
        else -> null
      }
      false -> null
    }
    else -> null
  }
  else -> null
}

internal fun localizedNameForRegion_short_vec(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "UK" // GB
    139 -> "Hong Kong" // HK
    193 -> "Macào" // MO
    229 -> "Palestina" // PS
    297 -> "USA" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_vi(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    139 -> "Hồng Kông" // HK
    193 -> "Macao" // MO
    229 -> "Palestine" // PS
    296 -> "LHQ" // UN
    297 -> "Mỹ" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_wae(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    139 -> "Hongkong" // HK
    193 -> "Makau" // MO
    else -> null
  }
}

internal fun localizedNameForRegion_short_wo(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    229 -> "Palestine" // PS
    else -> null
  }
}

internal fun localizedNameForRegion_short_xh(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "E-UK" // GB
    139 -> "EHong Kong" // HK
    193 -> "EMacao" // MO
    229 -> "EPalestina" // PS
    297 -> "I-US" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_xnr(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "यू॰के॰" // GB
    139 -> "हाँग काँग" // HK
    193 -> "मकाऊ" // MO
    229 -> "फ़िलिस्तीन" // PS
    297 -> "अमरिका" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_yi(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    297 -> "פֿ\"ש" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_yo(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? = when (region) {
  // yo
  0 -> when (script == 0 && variant == 0) {
    true -> when (query) {
      139 -> "Hong Kong" // HK
      193 -> "Màkáò" // MO
      229 -> "Palẹsítínì" // PS
      else -> null
    }
    false -> null
  }
  // yo-BJ
  59 -> when (script == 0 && variant == 0) {
    true -> when (query) {
      139 -> "Hong Kong" // HK
      193 -> "Màkáò" // MO
      229 -> "Palɛsítínì" // PS
      else -> null
    }
    false -> null
  }
  else -> null
}

internal fun localizedNameForRegion_short_yrl(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    139 -> "Hũgi Kũgi" // HK
    193 -> "Makau" // MO
    229 -> "Paretina" // PS
    296 -> "ONU" // UN
    297 -> "EUA" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_yue(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? = when (region) {
  // region = <none>
  0 -> when (script) {
    // yue
    0 -> when (variant == 0) {
      true -> when (query) {
        139 -> "香港" // HK
        193 -> "澳門" // MO
        229 -> "巴勒斯坦" // PS
        else -> null
      }
      false -> null
    }
    // yue-Hans
    21 -> when (variant == 0) {
      true -> when (query) {
        139 -> "香港" // HK
        193 -> "澳门" // MO
        229 -> "巴勒斯坦" // PS
        else -> null
      }
      false -> null
    }
    else -> null
  }
  else -> null
}

internal fun localizedNameForRegion_short_za(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    139 -> "Yanghgangj" // HK
    193 -> "Aumwnz" // MO
    229 -> "Bahlwzswhdanj" // PS
    else -> null
  }
}

internal fun localizedNameForRegion_short_zh(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? = when (region) {
  // region = <none>
  0 -> when (script) {
    // zh
    0 -> when (variant == 0) {
      true -> when (query) {
        139 -> "香港" // HK
        193 -> "澳门" // MO
        229 -> "巴勒斯坦" // PS
        else -> null
      }
      false -> null
    }
    // zh-Hant
    22 -> when (variant == 0) {
      true -> when (query) {
        139 -> "香港" // HK
        193 -> "澳門" // MO
        229 -> "巴勒斯坦" // PS
        else -> null
      }
      false -> null
    }
    else -> null
  }
  else -> null
}

internal fun localizedNameForRegion_short_zu(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "i-U.K." // GB
    139 -> "i-Hong Kong" // HK
    193 -> "i-Macau" // MO
    229 -> "i-Palestine" // PS
    296 -> "ifulegi" // UN
    297 -> "i-U.S" // US
    else -> null
  }
}
