// This file was automatically generated using the 'generateCode' Gradle task. Do not edit manually.
package io.fluidsonic.i18n.`data`

import kotlin.Int
import kotlin.String

internal fun localizedNameForRegion_short_fy(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "VK" // GB
    139 -> "Hongkong" // HK
    193 -> "Macao" // MO
    229 -> "Palestina" // PS
    297 -> "VS" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_ga(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "RA" // GB
    139 -> "Hong Cong" // HK
    193 -> "Macao" // MO
    229 -> "an Phalaistín" // PS
    296 -> "NA" // UN
    297 -> "SAM" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_gaa(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    193 -> "Makao" // MO
    297 -> "US" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_gd(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "RA" // GB
    139 -> "Hong Kong" // HK
    193 -> "Macàthu" // MO
    229 -> "Palastain" // PS
    297 -> "SA" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_gl(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "RU" // GB
    139 -> "Hong Kong" // HK
    193 -> "Macau" // MO
    229 -> "Palestina" // PS
    296 -> "ONU" // UN
    297 -> "EUA" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_gn(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    297 -> "EE. UU." // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_gsw(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    139 -> "Hongkong" // HK
    193 -> "Macao" // MO
    297 -> "USA" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_gu(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "યુ.કે." // GB
    139 -> "હોંગકોંગ" // HK
    193 -> "મકાઉ" // MO
    229 -> "પેલેસ્ટાઇન" // PS
    296 -> "યૂએન" // UN
    297 -> "યુ.એસ." // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_ha(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? = when (region) {
  // ha
  0 -> when (script == 0 && variant == 0) {
    true -> when (query) {
      139 -> "Hong Kong" // HK
      193 -> "Macao" // MO
      229 -> "Palasɗinu" // PS
      else -> null
    }
    false -> null
  }
  // ha-NE
  207 -> when (script == 0 && variant == 0) {
    true -> when (query) {
      139 -> "Hong Kong" // HK
      193 -> "Macao" // MO
      229 -> "Palasɗinu" // PS
      else -> null
    }
    false -> null
  }
  else -> null
}

internal fun localizedNameForRegion_short_he(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    139 -> "הונג קונג" // HK
    193 -> "מקאו" // MO
    229 -> "פלסטין" // PS
    297 -> "ארה״ב" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_hi(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? = when (region) {
  // region = <none>
  0 -> when (script) {
    // hi
    0 -> when (variant == 0) {
      true -> when (query) {
        121 -> "यू॰के॰" // GB
        139 -> "हाँग काँग" // HK
        193 -> "मकाऊ" // MO
        229 -> "फ़िलिस्तीन" // PS
        297 -> "अमेरिका" // US
        else -> null
      }
      false -> null
    }
    // hi-Latn
    3 -> when (variant == 0) {
      true -> when (query) {
        297 -> "America" // US
        else -> null
      }
      false -> null
    }
    else -> null
  }
  else -> null
}

internal fun localizedNameForRegion_short_hr(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "UK" // GB
    139 -> "Hong Kong" // HK
    193 -> "Makao" // MO
    229 -> "Palestina" // PS
    296 -> "UN" // UN
    297 -> "SAD" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_hsb(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "UK" // GB
    139 -> "Hongkong" // HK
    193 -> "Macao" // MO
    229 -> "Palestina" // PS
    297 -> "USA" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_hu(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "UK" // GB
    139 -> "Hongkong" // HK
    193 -> "Makaó" // MO
    229 -> "Palesztina" // PS
    296 -> "ENSZ" // UN
    297 -> "USA" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_hy(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "ՄԹ" // GB
    139 -> "Հոնկոնգ" // HK
    193 -> "Մակաո" // MO
    229 -> "Պաղեստին" // PS
    296 -> "ՄԱԿ" // UN
    297 -> "ԱՄՆ" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_ia(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "RU" // GB
    139 -> "Hongkong" // HK
    193 -> "Macao" // MO
    229 -> "Palestina" // PS
    297 -> "SUA" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_id(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "UK" // GB
    139 -> "Hong Kong" // HK
    193 -> "Makau" // MO
    229 -> "Palestina" // PS
    296 -> "PBB" // UN
    297 -> "AS" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_ie(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "UR" // GB
    139 -> "Hong-Kong" // HK
    193 -> "Macao" // MO
    229 -> "Palestina" // PS
    297 -> "US" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_ig(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "UK" // GB
    139 -> "Hong Kong" // HK
    193 -> "Macao" // MO
    229 -> "Palestine" // PS
    else -> null
  }
}

internal fun localizedNameForRegion_short_is(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    139 -> "Hong Kong" // HK
    193 -> "Makaó" // MO
    229 -> "Palestína" // PS
    296 -> "SÞ" // UN
    297 -> "BNA" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_it(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "UK" // GB
    139 -> "Hong Kong" // HK
    193 -> "Macao" // MO
    229 -> "Palestina" // PS
    296 -> "ONU" // UN
    297 -> "USA" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_ja(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "英国" // GB
    139 -> "香港" // HK
    193 -> "マカオ" // MO
    229 -> "パレスチナ" // PS
    296 -> "国連" // UN
    297 -> "アメリカ" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_jv(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "KM" // GB
    139 -> "Hong Kong" // HK
    193 -> "Macau" // MO
    229 -> "Palèstina" // PS
    297 -> "AS" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_ka(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "გაერთ.სამ." // GB
    139 -> "ჰონკონგი" // HK
    193 -> "მაკაო" // MO
    229 -> "პალესტინა" // PS
    297 -> "აშშ" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_kaa(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "Британия" // GB
    139 -> "Гонконг" // HK
    193 -> "Макао" // MO
    229 -> "Палестина" // PS
    297 -> "АҚШ" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_kab(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "TY" // GB
    139 -> "Hung Kung" // HK
    193 -> "Makaw" // MO
    229 -> "Palistin" // PS
    297 -> "Marikan" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_kea(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "R.U." // GB
    139 -> "Hong Kong" // HK
    193 -> "Makau" // MO
    229 -> "Palistina" // PS
    297 -> "S.U." // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_kgp(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    139 -> "Hãg Kãg" // HK
    193 -> "Mỹkav" // MO
    229 -> "Parenhtinỹ" // PS
    296 -> "ONU" // UN
    297 -> "EUA" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_kk(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? = when (region) {
  // region = <none>
  0 -> when (script) {
    // kk
    0 -> when (variant == 0) {
      true -> when (query) {
        139 -> "Сянган" // HK
        193 -> "Макао" // MO
        229 -> "Палестина" // PS
        296 -> "БҰҰ" // UN
        297 -> "АҚШ" // US
        else -> null
      }
      false -> null
    }
    // kk-Arab
    1 -> when (variant == 0) {
      true -> when (query) {
        229 -> "پالەستينا" // PS
        297 -> "ا ق ش" // US
        else -> null
      }
      false -> null
    }
    else -> null
  }
  else -> null
}

internal fun localizedNameForRegion_short_kl(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    297 -> "USA" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_km(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "ច.អ." // GB
    139 -> "ហុងកុង" // HK
    193 -> "ម៉ាកាវ" // MO
    229 -> "ប៉ាឡេស្ទីន" // PS
    296 -> "UN" // UN
    297 -> "ស.រ.អ" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_kn(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "ಯು.ಕೆ." // GB
    139 -> "ಹಾಂಗ್ ಕಾಂಗ್" // HK
    193 -> "ಮಕಾವು" // MO
    229 -> "ಪ್ಯಾಲೆಸ್ಟೈನ್" // PS
    296 -> "ಯುಎನ್" // UN
    297 -> "ಯು.ಎಸ್" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_ko(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    139 -> "홍콩" // HK
    167 -> "한국" // KR
    193 -> "마카오" // MO
    229 -> "팔레스타인" // PS
    296 -> "유엔" // UN
    else -> null
  }
}

internal fun localizedNameForRegion_short_kok(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? = when (region) {
  // region = <none>
  0 -> when (script) {
    // kok
    0 -> when (variant == 0) {
      true -> when (query) {
        121 -> "संयुक्त राजवट" // GB
        139 -> "हाँग काँग" // HK
        193 -> "मकाव" // MO
        229 -> "पालेसटीनी प्रांत" // PS
        297 -> "युएस" // US
        else -> null
      }
      false -> null
    }
    // kok-Latn
    3 -> when (variant == 0) {
      true -> when (query) {
        121 -> "Sõyukt Razvott" // GB
        139 -> "Hong Kong" // HK
        193 -> "Macao" // MO
        229 -> "Palestini Prant" // PS
        297 -> "Amerikechim Sõyukt Rajyam" // US
        else -> null
      }
      false -> null
    }
    else -> null
  }
  else -> null
}

internal fun localizedNameForRegion_short_ks(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    139 -> "ہانگ کانگ" // HK
    193 -> "ماکاوو" // MO
    229 -> "فلسطین" // PS
    else -> null
  }
}

internal fun localizedNameForRegion_short_ksh(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    297 -> "USA" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_ku(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "QY" // GB
    139 -> "Hong Kong" // HK
    193 -> "Makao" // MO
    229 -> "Filistîn" // PS
    297 -> "DAY" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_kxv(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? = when (region) {
  // region = <none>
  0 -> when (script) {
    // kxv
    0 -> when (variant == 0) {
      true -> when (query) {
        121 -> "yuke" // GB
        139 -> "hong kong" // HK
        193 -> "makao" // MO
        229 -> "palesṭain" // PS
        297 -> "yu.es." // US
        else -> null
      }
      false -> null
    }
    // kxv-Deva
    9 -> when (variant == 0) {
      true -> when (query) {
        121 -> "यू॰के॰" // GB
        139 -> "हाँग काँग" // HK
        193 -> "मकाऊ" // MO
        229 -> "प़िलिस्तीन" // PS
        297 -> "यू॰एस॰" // US
        else -> null
      }
      false -> null
    }
    // kxv-Orya
    10 -> when (variant == 0) {
      true -> when (query) {
        121 -> "ୟୁକେ" // GB
        139 -> "ହଂଗ କଂଗ" // HK
        193 -> "ମକାଉ" // MO
        229 -> "ପାଲେସ୍ଟାଇନ" // PS
        297 -> "ୟୁ ଏସ୍" // US
        else -> null
      }
      false -> null
    }
    // kxv-Telu
    11 -> when (variant == 0) {
      true -> when (query) {
        121 -> "యు.కె." // GB
        139 -> "హంకం" // HK
        193 -> "మకాఉ" // MO
        229 -> "పాలస్తీనా" // PS
        297 -> "యు.ఎస్." // US
        else -> null
      }
      false -> null
    }
    else -> null
  }
  else -> null
}

internal fun localizedNameForRegion_short_ky(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "УБ" // GB
    139 -> "Гонконг" // HK
    193 -> "Макао" // MO
    229 -> "Палестина" // PS
    297 -> "АКШ" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_lb(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "GB" // GB
    139 -> "Hong Kong" // HK
    193 -> "Macau" // MO
    229 -> "Palestina" // PS
    297 -> "US" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_lij(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "RU" // GB
    139 -> "Hong Kong" // HK
    193 -> "Macao" // MO
    229 -> "Palestiña" // PS
    297 -> "SUA" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_lld(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "UK" // GB
    139 -> "Hong Kong" // HK
    193 -> "Macao" // MO
    229 -> "Palestina" // PS
    else -> null
  }
}

internal fun localizedNameForRegion_short_ln(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    139 -> "Hong Kong" // HK
    193 -> "Makau" // MO
    else -> null
  }
}

internal fun localizedNameForRegion_short_lo(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "ສະຫະລາຊະອານາຈັກ" // GB
    139 -> "ຮົງກົງ" // HK
    193 -> "ມາກາວ" // MO
    229 -> "ປາເລດສະຕິນ" // PS
    296 -> "ຢູເອັນ" // UN
    else -> null
  }
}

internal fun localizedNameForRegion_short_lt(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "JK" // GB
    139 -> "Honkongas" // HK
    193 -> "Makao" // MO
    229 -> "Palestina" // PS
    296 -> "JT" // UN
    297 -> "JAV" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_lv(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    139 -> "Honkonga" // HK
    193 -> "Makao" // MO
    229 -> "Palestīna" // PS
    296 -> "ANO" // UN
    297 -> "ASV" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_mai(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "यू॰के॰" // GB
    139 -> "हांग कांग" // HK
    193 -> "मकाउ" // MO
    229 -> "फिलिस्तीन" // PS
    else -> null
  }
}

internal fun localizedNameForRegion_short_mi(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    139 -> "Hongipua" // HK
    193 -> "Makau" // MO
    229 -> "Parihitini" // PS
    else -> null
  }
}

internal fun localizedNameForRegion_short_mk(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "ОК" // GB
    139 -> "Хонгконг" // HK
    193 -> "Макао" // MO
    229 -> "Палестина" // PS
    296 -> "ОН" // UN
    297 -> "САД" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_ml(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "യുകെ" // GB
    139 -> "ഹോങ്കോങ്" // HK
    193 -> "മക്കാവു" // MO
    229 -> "പലസ്‌തീൻ" // PS
    296 -> "യുഎൻ" // UN
    297 -> "യു.എസ്" // US
    else -> null
  }
}
