// This file was automatically generated using the 'generateCode' Gradle task. Do not edit manually.
package io.fluidsonic.i18n.`data`

import kotlin.Int
import kotlin.String

internal fun localizedNameForRegion_short_ab(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "Британиа" // GB
    139 -> "Гонконг" // HK
    193 -> "Макао" // MO
    229 -> "Палестина" // PS
    297 -> "ЕАШ" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_af(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "VK" // GB
    139 -> "Hongkong" // HK
    193 -> "Macau" // MO
    229 -> "Palestina" // PS
    296 -> "VN" // UN
    297 -> "VSA" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_ak(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    139 -> "Hɔnkɔn" // HK
    193 -> "Makaw" // MO
    else -> null
  }
}

internal fun localizedNameForRegion_short_am(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "ዩኬ" // GB
    139 -> "ሆንግ ኮንግ" // HK
    193 -> "ማካኦ" // MO
    229 -> "ፍልስጥኤም" // PS
    296 -> "የተመ" // UN
    else -> null
  }
}

internal fun localizedNameForRegion_short_an(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "RU" // GB
    139 -> "Hong Kong" // HK
    193 -> "Macau" // MO
    229 -> "Palestina" // PS
    297 -> "EUA" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_ar(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? = when (region) {
  // ar
  0 -> when (script == 0 && variant == 0) {
    true -> when (query) {
      139 -> "هونغ كونغ" // HK
      193 -> "مكاو" // MO
      229 -> "فلسطين" // PS
      else -> null
    }
    false -> null
  }
  // ar-SA
  253 -> when (script == 0 && variant == 0) {
    true -> when (query) {
      193 -> "ماكاو" // MO
      else -> null
    }
    false -> null
  }
  else -> null
}

internal fun localizedNameForRegion_short_as(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "ইউ. কে." // GB
    139 -> "হং কং" // HK
    193 -> "মাকাও" // MO
    229 -> "ফিলিস্তিন" // PS
    297 -> "মা. যু." // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_ast(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "RX" // GB
    139 -> "Ḥong Kong" // HK
    193 -> "Macáu" // MO
    229 -> "Palestina" // PS
    297 -> "EE.XX." // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_az(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? = when (region) {
  // region = <none>
  0 -> when (script) {
    // az
    0 -> when (variant == 0) {
      true -> when (query) {
        121 -> "BK" // GB
        139 -> "Honq Konq" // HK
        193 -> "Makao" // MO
        229 -> "Fələstin" // PS
        296 -> "BMT" // UN
        297 -> "ABŞ" // US
        else -> null
      }
      false -> null
    }
    // az-Cyrl
    2 -> when (variant == 0) {
      true -> when (query) {
        121 -> "БК" // GB
        139 -> "Һонг Конг" // HK
        193 -> "Макао" // MO
        297 -> "АБШ" // US
        else -> null
      }
      false -> null
    }
    else -> null
  }
  else -> null
}

internal fun localizedNameForRegion_short_ba(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "Британия" // GB
    139 -> "Гонконг" // HK
    193 -> "Макао" // MO
    229 -> "Фәләстин" // PS
    297 -> "АҠШ" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_bal(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 3 || variant != 0) {
    return null
  }
  return when (query) {
    139 -> "Háng Káng" // HK
    193 -> "Makaó" // MO
    229 -> "Palastin" // PS
    else -> null
  }
}

internal fun localizedNameForRegion_short_be(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? = when (region) {
  // region = <none>
  0 -> when (script) {
    // script = <none>
    0 -> when (variant) {
      0 -> when (query) {
        139 -> "Ганконг" // HK
        193 -> "Макаа" // MO
        229 -> "Палесціна" // PS
        297 -> "ЗША" // US
        else -> null
      }
      1 -> when (query) {
        193 -> "Макао" // MO
        229 -> "Палестына" // PS
        else -> null
      }
      else -> null
    }
    else -> null
  }
  else -> null
}

internal fun localizedNameForRegion_short_bew(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "KR" // GB
    139 -> "Hongkong" // HK
    193 -> "Makao" // MO
    229 -> "Palèstina" // PS
    297 -> "AS" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_bg(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    139 -> "Хонконг" // HK
    193 -> "Макао" // MO
    229 -> "Палестина" // PS
    297 -> "САЩ" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_blo(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "GT" // GB
    139 -> "Hɔŋ Kɔŋ" // HK
    193 -> "Makawoo" // MO
    229 -> "Palɛstiin" // PS
    297 -> "GKA" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_bn(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "ইউ কে" // GB
    139 -> "হংকং" // HK
    193 -> "ম্যাকাও" // MO
    229 -> "ফিলিস্তিন" // PS
    297 -> "ইউ এস" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_br(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "RU" // GB
    139 -> "Hong Kong" // HK
    193 -> "Makao" // MO
    229 -> "Palestina" // PS
    297 -> "SU" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_brx(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "इउ.के" // GB
    139 -> "हंकं" // HK
    193 -> "माकाउ" // MO
    229 -> "पेलेस्ताइन" // PS
    297 -> "इउ. एस" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_bs(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? = when (region) {
  // region = <none>
  0 -> when (script) {
    // bs
    0 -> when (variant == 0) {
      true -> when (query) {
        121 -> "UK" // GB
        139 -> "Hong Kong" // HK
        193 -> "Makao" // MO
        229 -> "Palestina" // PS
        296 -> "UN" // UN
        297 -> "SAD" // US
        else -> null
      }
      false -> null
    }
    // bs-Cyrl
    2 -> when (variant == 0) {
      true -> when (query) {
        121 -> "УК" // GB
        139 -> "Хонг Конг" // HK
        193 -> "Макао" // MO
        229 -> "Палестина" // PS
        297 -> "САД" // US
        else -> null
      }
      false -> null
    }
    else -> null
  }
  else -> null
}

internal fun localizedNameForRegion_short_ca(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "RU" // GB
    139 -> "Hong Kong" // HK
    193 -> "Macau" // MO
    229 -> "Palestina" // PS
    296 -> "ONU" // UN
    297 -> "EUA" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_ccp(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "𑄄𑄃𑄪𑄇𑄬" // GB
    139 -> "𑄦𑄧𑄁𑄇𑄧𑄁" // HK
    193 -> "𑄟𑄳𑄠𑄇𑄃𑄮" // MO
    229 -> "𑄜𑄨𑄣𑄨𑄌𑄴𑄖𑄨𑄚𑄴" // PS
    296 -> "𑄃𑄨𑄃𑄪𑄃𑄬𑄚𑄴" // UN
    297 -> "𑄃𑄨𑄃𑄪𑄃𑄬𑄌𑄴" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_ce(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    139 -> "Гонконг" // HK
    193 -> "Макао" // MO
    229 -> "ПалестӀина" // PS
    297 -> "АЦШ" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_ceb(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "UK" // GB
    139 -> "Hong Kong" // HK
    193 -> "Macao" // MO
    229 -> "Palestine" // PS
    297 -> "US" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_chr(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    139 -> "ᎰᏂᎩ ᎪᏂᎩ" // HK
    193 -> "ᎹᎧᎣ" // MO
    229 -> "ᏆᎴᏍᏗᏂ" // PS
    297 -> "US" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_ckb(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    229 -> "فەلەستین" // PS
    else -> null
  }
}

internal fun localizedNameForRegion_short_co(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "R.U." // GB
    193 -> "Macao" // MO
    297 -> "S.U." // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_cop(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "ϯⲙⲉⲧⲟⲩⲣⲟ ⲉⲧϩⲱⲧⲡ" // GB
    139 -> "ϯϩⲟⲛⲅ ⲕⲟⲛⲅ" // HK
    193 -> "ϯⲙⲁⲕⲁⲟ" // MO
    229 -> "ⲫⲩⲗⲓⲥⲧⲓⲓⲙ" // PS
    297 -> "ⲛⲓⲑⲱϣ ⲉⲧϩⲱⲧⲡ" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_cs(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "GB" // GB
    139 -> "Hongkong" // HK
    193 -> "Macao" // MO
    229 -> "Palestina" // PS
    296 -> "OSN" // UN
    297 -> "USA" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_csw(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    297 -> "ᑭᐦᒋ ᒨᑯᒫᓂᕽ" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_cv(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "Британи" // GB
    139 -> "Гонконг" // HK
    193 -> "Макао" // MO
    229 -> "Палестина" // PS
    297 -> "АПШ" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_cy(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "DU" // GB
    139 -> "Hong Kong" // HK
    193 -> "Macau" // MO
    229 -> "Palesteina" // PS
    296 -> "CU" // UN
    297 -> "UDA" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_da(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    139 -> "Hongkong" // HK
    193 -> "Macao" // MO
    229 -> "Palæstina" // PS
    296 -> "FN" // UN
    else -> null
  }
}

internal fun localizedNameForRegion_short_de(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "UK" // GB
    139 -> "Hongkong" // HK
    193 -> "Macau" // MO
    229 -> "Palästina" // PS
    296 -> "UN" // UN
    297 -> "USA" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_dsb(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "UK" // GB
    139 -> "Hongkong" // HK
    193 -> "Macao" // MO
    229 -> "Palestina" // PS
    297 -> "USA" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_dz(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    139 -> "ཧོང་ཀོང" // HK
    193 -> "མཀ་ཨའུ" // MO
    else -> null
  }
}

internal fun localizedNameForRegion_short_ee(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    139 -> "Hɔng Kɔng nutome" // HK
    193 -> "Macau nutome" // MO
    else -> null
  }
}

internal fun localizedNameForRegion_short_el(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "ΗΒ" // GB
    139 -> "Χονγκ Κονγκ" // HK
    193 -> "Μακάο" // MO
    229 -> "Παλαιστίνη" // PS
    296 -> "ΟΗΕ" // UN
    297 -> "ΗΠΑ" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_en(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? = when (region) {
  // region = <none>
  0 -> when (script) {
    // en
    0 -> when (variant == 0) {
      true -> when (query) {
        51 -> "Bosnia" // BA
        74 -> "Cocos Islands" // CC
        121 -> "UK" // GB
        139 -> "Hong Kong" // HK
        191 -> "Myanmar" // MM
        193 -> "Macao" // MO
        227 -> "Pitcairn" // PN
        229 -> "Palestine" // PS
        296 -> "UN" // UN
        297 -> "US" // US
        else -> null
      }
      false -> null
    }
    // en-Dsrt
    5 -> when (variant == 0) {
      true -> when (query) {
        139 -> "𐐐𐐬𐑍 𐐗𐐬𐑍" // HK
        193 -> "𐐣𐐲𐐿𐐵" // MO
        else -> null
      }
      false -> null
    }
    // en-Shaw
    6 -> when (variant == 0) {
      true -> when (query) {
        121 -> "·𐑿𐑒𐑱" // GB
        139 -> "·𐑣𐑪𐑙 𐑒𐑪𐑙" // HK
        193 -> "·𐑥𐑩𐑒𐑬" // MO
        229 -> "·𐑐𐑨𐑤𐑩𐑕𐑑𐑲𐑯" // PS
        297 -> "·𐑿𐑧𐑕" // US
        else -> null
      }
      false -> null
    }
    else -> null
  }
  // en-CA
  73 -> when (script == 0 && variant == 0) {
    true -> when (query) {
      193 -> "Macau" // MO
      else -> null
    }
    false -> null
  }
  else -> null
}

internal fun localizedNameForRegion_short_eo(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "Britujo" // GB
    else -> null
  }
}

internal fun localizedNameForRegion_short_es(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? = when (region) {
  // es
  0 -> when (script == 0 && variant == 0) {
    true -> when (query) {
      121 -> "RU" // GB
      139 -> "Hong Kong" // HK
      193 -> "Macao" // MO
      229 -> "Palestina" // PS
      297 -> "EE. UU." // US
      else -> null
    }
    false -> null
  }
  // es-419
  31 -> when (script == 0 && variant == 0) {
    true -> when (query) {
      121 -> "R. U." // GB
      else -> null
    }
    false -> null
  }
  // es-AR
  44 -> when (script == 0 && variant == 0) {
    true -> when (query) {
      121 -> "RU" // GB
      else -> null
    }
    false -> null
  }
  // es-BO
  63 -> when (script == 0 && variant == 0) {
    true -> when (query) {
      121 -> "RU" // GB
      else -> null
    }
    false -> null
  }
  // es-CL
  81 -> when (script == 0 && variant == 0) {
    true -> when (query) {
      121 -> "RU" // GB
      else -> null
    }
    false -> null
  }
  // es-CO
  84 -> when (script == 0 && variant == 0) {
    true -> when (query) {
      121 -> "RU" // GB
      else -> null
    }
    false -> null
  }
  // es-CR
  87 -> when (script == 0 && variant == 0) {
    true -> when (query) {
      121 -> "RU" // GB
      else -> null
    }
    false -> null
  }
  // es-DO
  101 -> when (script == 0 && variant == 0) {
    true -> when (query) {
      121 -> "RU" // GB
      else -> null
    }
    false -> null
  }
  // es-EC
  104 -> when (script == 0 && variant == 0) {
    true -> when (query) {
      121 -> "RU" // GB
      else -> null
    }
    false -> null
  }
  // es-GT
  135 -> when (script == 0 && variant == 0) {
    true -> when (query) {
      121 -> "RU" // GB
      else -> null
    }
    false -> null
  }
  // es-HN
  141 -> when (script == 0 && variant == 0) {
    true -> when (query) {
      121 -> "RU" // GB
      else -> null
    }
    false -> null
  }
  // es-MX
  202 -> when (script == 0 && variant == 0) {
    true -> when (query) {
      121 -> "RU" // GB
      else -> null
    }
    false -> null
  }
  // es-NI
  210 -> when (script == 0 && variant == 0) {
    true -> when (query) {
      121 -> "RU" // GB
      else -> null
    }
    false -> null
  }
  // es-PA
  219 -> when (script == 0 && variant == 0) {
    true -> when (query) {
      121 -> "RU" // GB
      else -> null
    }
    false -> null
  }
  // es-PE
  220 -> when (script == 0 && variant == 0) {
    true -> when (query) {
      121 -> "RU" // GB
      else -> null
    }
    false -> null
  }
  // es-PY
  232 -> when (script == 0 && variant == 0) {
    true -> when (query) {
      121 -> "RU" // GB
      else -> null
    }
    false -> null
  }
  // es-US
  297 -> when (script == 0 && variant == 0) {
    true -> when (query) {
      121 -> "RU" // GB
      else -> null
    }
    false -> null
  }
  // es-VE
  302 -> when (script == 0 && variant == 0) {
    true -> when (query) {
      121 -> "RU" // GB
      else -> null
    }
    false -> null
  }
  else -> null
}

internal fun localizedNameForRegion_short_et(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "ÜK" // GB
    139 -> "Hongkong" // HK
    193 -> "Macau" // MO
    229 -> "Palestiina" // PS
    296 -> "ÜRO" // UN
    297 -> "USA" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_eu(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "EB" // GB
    139 -> "Hong Kong" // HK
    193 -> "Macau" // MO
    229 -> "Palestina" // PS
    297 -> "AEB" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_fa(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? = when (region) {
  // fa
  0 -> when (script == 0 && variant == 0) {
    true -> when (query) {
      139 -> "هنگ‌کنگ" // HK
      193 -> "ماکائو" // MO
      229 -> "فلسطین" // PS
      253 -> "عربستان" // SA
      296 -> "سازمان ملل" // UN
      else -> null
    }
    false -> null
  }
  // fa-AF
  36 -> when (script == 0 && variant == 0) {
    true -> when (query) {
      139 -> "هانگ کانگ" // HK
      else -> null
    }
    false -> null
  }
  else -> null
}

internal fun localizedNameForRegion_short_ff(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 7 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "𞤑𞤘" // GB
    139 -> "𞤖𞤮𞤲𞤺 𞤑𞤮𞤲𞤺" // HK
    193 -> "𞤃𞤢𞤳𞤢𞤱𞤮𞥅" // MO
    297 -> "𞤁𞤂𞤀" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_fi(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "Britannia" // GB
    139 -> "Hongkong" // HK
    193 -> "Macao" // MO
    229 -> "Palestiina" // PS
    296 -> "YK" // UN
    297 -> "USA" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_fil(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "UK" // GB
    139 -> "Hong Kong" // HK
    193 -> "Macau" // MO
    229 -> "Palestine" // PS
    296 -> "UN" // UN
    297 -> "U.S." // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_fo(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "UK" // GB
    139 -> "Hong Kong" // HK
    193 -> "Makao" // MO
    229 -> "Palestina" // PS
    297 -> "USA" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_fr(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "R.-U." // GB
    139 -> "Hong Kong" // HK
    193 -> "Macao" // MO
    229 -> "Palestine" // PS
    296 -> "ONU" // UN
    297 -> "É.-U." // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_frr(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "UK" // GB
    139 -> "Hongkong" // HK
    193 -> "Macao" // MO
    229 -> "Palestiina" // PS
    297 -> "USA" // US
    else -> null
  }
}

internal fun localizedNameForRegion_short_fur(
  query: Int,
  script: Int,
  region: Int,
  variant: Int,
): String? {
  if (region != 0 || script != 0 || variant != 0) {
    return null
  }
  return when (query) {
    121 -> "Ream Unît" // GB
    139 -> "Hong Kong" // HK
    193 -> "Macao" // MO
    else -> null
  }
}
